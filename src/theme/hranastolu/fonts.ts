import { Archivo_Black, Inter } from 'next/font/google';

export const InterFont = Inter({ subsets: ['latin-ext'], weight: ["400", "700"] });
export const ArchivoBlackFont = Archivo_Black({ subsets: ['latin-ext'], weight: ["400"] });
